// Basic Card constructor to take in the "front" & "back" of card
var BasicCard = function(front, back) {
	this.front = front;
	this.back = back;
}; 

// Export front & back variables
module.exports = BasicCard;